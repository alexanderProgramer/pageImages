make a this command for you local


npm list -g http-server

Si no está instalado, instálalo globalmente con:
npm install -g http-server


3. Verifica la configuración de tu PATH:

Asegúrate de que la carpeta donde npm instala los paquetes globales esté en tu variable de entorno PATH.

Puedes verificar la ubicación de los paquetes globales de npm con:
npm config get prefix

4. Ejecuta el comando desde la carpeta de npm:

Navega a la carpeta donde npm instala los paquetes globales y ejecuta el comando directamente desde allí:


Ejecuta el comando desde la carpeta de npm:

Navega a la carpeta donde npm instala los paquetes globales y ejecuta el comando directamente desde allí:

cd C:\Users\TuUsuario\AppData\Roaming\npm
http-server -p 8000


and you install live server , in your visual study code
after you must , use comando
ctrl + shift + x
install,  live server

so you search your index, and click right and
live in server